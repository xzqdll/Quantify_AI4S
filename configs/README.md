# Configurations

Store version-controlled YAML or TOML files for data collection, detector
adaptation, regression specifications, topic clustering, stylometric analysis,
robustness checks, and figure generation. Credentials belong in `.env`, not in
configuration files.

